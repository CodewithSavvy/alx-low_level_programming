#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 *@n: the number to be absoluted
 *Return: 0
 */

int _abs(int n)
{
	int n = 0;

	if (n < 0)
		return (n);
	else
		return (-n)
}
