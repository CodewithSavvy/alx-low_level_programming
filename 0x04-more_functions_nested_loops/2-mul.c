#include "main.h"
/**
 * mul - prints the multiplication of two numbers
 * @a:first digit to be multiply
 * @b: second digit to be mulitiply
 *Return: 0
 */

int mul(int a, int b)
{
	return (a * b);
}
